@import org.shuzu.generator.*
@extends("layout")
@args Site site
@args Organization org

@for(Repository repo: org.repos@) {
<div>
    <div><a href="/@site.repoPath(org, repo)">@repo.getName()</a></div>
    <div>@repo.getDescription()</div>
</div>
}
